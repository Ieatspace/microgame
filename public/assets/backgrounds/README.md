Drop replacement battlefield and parallax backgrounds here.

The default battlefield now uses procedural tile assets from `lib/pixelArtGenerator.ts`
through `lib/spriteSystem.ts`: sky tiles, dirt path tiles, stone road tiles,
distant towers, village huts, moon, spawn gate, and magic circle.

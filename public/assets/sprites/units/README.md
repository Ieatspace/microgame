Drop replacement unit sprite sheets here.

The default game now renders cached procedural pixel-art sprites from `lib/pixelArtGenerator.ts`.
Optional AI-generated files in `public/assets/sprites/` can override selected units:

- `wizard.png`
- `archer.png`
- `knight.png`
- `catapult.png`

Keep assets square, transparent, and pixel-art styled so they match the procedural fallback.

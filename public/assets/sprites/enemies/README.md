Drop replacement enemy sprite sheets here.

The default game uses generated 32x32 pixel sprites with the bottom center as the baseline.
Optional AI-generated files in `public/assets/sprites/` can override enemy art:

- `knight.png`
- `boss.png`

Keep each sprite square, transparent, high-contrast, and aligned to the bottom-center baseline.

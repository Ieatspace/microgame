Drop replacement projectile and impact sprite sheets here.

The default game renders generated pixel-art impact and projectile effects from
`lib/pixelArtGenerator.ts`, then caches them through `lib/spriteSystem.ts`.
